.DS_Store
.env
.env.*
!.env.example

# Real PCA RMR season data — may contain member PII (names, emails, member #s).
# Local dev only. See feedback_sensitive_axdb_fixtures memory.
real_season_data/

# Scraped RMsolo results PDFs (public data, but no need to version large binaries).
rmsolo_data/

# claude config / scratch
.claude/

# deps & build output (workspace-wide)
node_modules/

# pnpm
.pnpm-store/

# local databases (Prisma SQLite dev/test files)
*.db
*.db-journal

# generated test fixtures (rebuilt by `pnpm --filter web pretest`)
apps/web/tests/fixtures/season-event-*.axdb
apps/web/tests/fixtures/multi-event-rejection.axdb
apps/web/tests/fixtures/synthetic.axdb
apps/web/tests/fixtures/combined-event-*.axdb

# local docs (private, not for public consumption)
docs/private/

# logos etc
assets/
